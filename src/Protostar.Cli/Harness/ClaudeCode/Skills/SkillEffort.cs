namespace Protostar.Cli.Harness.ClaudeCode;

/// <summary>The effort level a skill requests while active (Claude Code's <c>effort</c> field).</summary>
internal enum SkillEffort
{
    Low,
    Medium,
    High,
    XHigh,
    Max,
}
